import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(0, 20, 100)
plt.plot(x, np.sin(x))
plt.show()

#go to the  ... at next to the search and create a new terminal. Then type py -3 -m venv introvenv. In explorer, it sould be available
#now you can switch interpreteres. 
#if you want to install a library a library, the code is: pip install (name of library)